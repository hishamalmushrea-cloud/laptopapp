# خارطة الطريق / Roadmap

مرتّبة حسب **الأثر ÷ الجهد**. البنود 1–2 أساسية قبل أي ميزة.

## المرحلة 0 — الثقة (لا ميزات جديدة قبلها)

### 0. تحقق من سلامة المكوّنات المُنزَّلة — **تم**
- [x] كان `GeneralComponents.java:28` ينزّل من فرع `main` المتحرّك **بلا أي تحقق**
      (لا `MessageDigest` ولا `sha256` في كل الـ 267 ملف Java).
- [x] الرابط ثُبِّت على كوميت، وأُضيف `ComponentChecksums` + `assets/component_checksums.json`
      (‏19 بصمة من `scripts/assets.sha256`). الملف المرفوض يُحذف ولا يُثبَّت.
- [x] التجزئة تعمل خارج خيط الواجهة (‏`HttpUtils` يسلّم النتيجة عبر `runOnUiThread`).

### 0ب. واجهة عربية — **تم**
- [x] `values-ar/strings.xml` ‏(‏286 نصًا = كل القابل للترجمة)، و`android:supportsRtl="true"`
      الذي كان **مفقودًا** — بدونه كانت العربية ستُعرض من اليسار لليمين.

### 1. CI + بصمات + توقيع
- [x] `scripts/fetch-assets.sh` يتحقق من SHA-256 لكل ملف ثنائي (73 ملفًا).
- [x] `ci/build.yml` مكتوب ومختبَر الصيغة (وظيفتان: `assets` ثم `apk` مع نشر `SHA256SUMS.txt`).
- [ ] **تفعيله**: `scripts/install-ci.sh` ثم push بتوكن يملك صلاحية `workflows`
      (التوكن الآلي الحالي لا يملكها — GitHub رفض الـ push صراحةً).
- [ ] إضافة `assembleRelease` موقّع على كل وسم.
- [ ] *لماذا أولًا:* upstream بلا أي CI، وAPK بحجم ~150 MB يُبنى يدويًا، ونسخ "معدّلة"
      منتشرة. هذه أرخص طريقة لإغلاق أخطر ثغرة ثقة.

### 2. اختبارات للمنطق القابل للاختبار — **بدأ**
- [x] `junit 4.13.2` + `ChecksumUtilsTest` (‏7 حالات) + خطوة `testDebugUnitTest` في CI
      قبل `assembleDebug`. أول اختبار ناجح في تاريخ المشروع.
- [x] ‏`KeyValueSetTest` ‏(‏10 حالات) — وأثناء كتابته ظهر **خللان حقيقيان** في `KeyValueSet`:
      مقطع بلا `=` كان يُسقط `substring(index+1, end)` باستثناء، و`indexOfKey` كان يستدعي
      `substring(start, -1)`؛ أي أن زوجًا واحدًا تالفًا كان يُفسد قراءة السلسلة كلها —
      وهذه السلاسل تأتي من بيانات مستوردة/مُنزَّلة. أُصلحا وثُبّتا باختبار.
- [x] تثبيت **آخر رابط متغيّر**: `WineUtils` كان يكتب `Software\\Wine\\AddonsURL` من فرع
      `main` المتحرّك (يُنزّل عبره Wine إضافتَي Mono وGecko، ≈82 + 53 ميغابايت). الكوميت
      أصبح `ComponentChecksums.UPSTREAM_COMMIT` ويُشار إليه من `WineUtils` و`GeneralComponents`
      معًا. **لم يعد في التطبيق أي تنزيل يتبع فرعًا متحرّكًا.**
- [ ] الخطوة التالية: `GraphicsDrivers.parseIdentifiers/parseConfigs` و`DefaultVersion.DXVK(...)`.

## المرحلة 1 — ميزات مطلوبة مجتمعيًا (من قضايا upstream)

| # | الميزة | قضية upstream | لماذا سهلة |
|---|---|---|---|
| 3 | ~~ملفات تعريف الألعاب مخزّنة في المستودع ولا تُستعمل~~ — **تم** | — | الـ 53 ملفًا في `input_controls/` صارت داخل APK في `assets/inputcontrols/library/` (‏220 KB)، والقراءة من الأصول بدل HTTP |
| 3ب | قاعدة ملفات تعريف لكل لعبة (إعدادات مثلى تلقائية حسب اسم الـ exe) | #2000 | الأساس موجود: `ShortcutSettingsDialog` يحفظ `controlsProfile` لكل اختصار |
| 4 | ~~تصدير/استيراد الحاوية~~ — **تم** | #2002 | أرشيف `.tzst` واحد عبر SAF؛ الاستيراد ينشئ حاوية جديدة ولا يلمس الموجودة |
| 5 | كشف تلقائي للكرت الرسومي واختيار التعريف الأمثل | #1998 | `GraphicsDrivers.getDefaultDriver()` يفعل هذا جزئيًا بالفعل + `assets/gpu_cards.json` (19 KB) |
| 6 | حوار تشخيص الأعطال مع التقاط مخرجات العملية | #2001 | `wine_debug_channels.json` موجود (5.8 KB) |

## المرحلة 2 — البنية

- [ ] **رفع `targetSdkVersion` عن 28.** حاليًا `targetSdk 28` مع `compileSdk 35` —
      هذا يمنع التوزيع عبر المتاجر ويرث `WRITE_EXTERNAL_STORAGE`. المسار: تنفيذ الملفات
      الثنائية من مجلد المكتبات الأصليّة داخل APK بدل مجلد البيانات.
- [ ] **تحديث تفاضلي / تنزيل المكوّنات عند الطلب** بدل إعادة تنزيل ~150 MB كل مرة.
      الأساس موجود: ميزة "تثبيت مكوّنات من ملف" في 11.0.
- [ ] `minifyEnabled true` في debug → تعطيله (يبطئ التطوير).
- [ ] إضافة `signingConfig` لـ release.

## المرحلة 3 — المجتمع

- [ ] منصة ترجمة (Weblate/Crowdin) بدل PRs الترجمة المتراكمة (upstream #1348 روسية، #75 صينية بـ 34 تعليقًا).
- [ ] قاعدة توافق: لعبة × جهاز × إعدادات × نتيجة.
- [ ] تفعيل GitHub Sponsors (ملف `FUNDING.yml` الحالي فيه `buy_me_a_coffee` فقط وبقية الحقول نصوص القالب).

---

### مقياس الإنجاز المقترح

لا يُقبل بند "منتهيًا" دون: اختبار آلي يمرّ في CI، أو لقطة شاشة من جهاز حقيقي
(يفضَّل Adreno + Mali + Xclipse لتغطية مسارات Vortek/Gladio الثلاثة).
